class LinkedLink {
	public:
		LinkedLink();
		int add(int);
		int remove(void);		
	private:
		int data;
		LinkedLink* head;
		LinkedLink* tail;
		LinkedLink* next;
};

LinkedLink::LinkedLink()
{
	next = NULL;
	head = NULL;
	tail = NULL;
}

int LinkedLink::add(int item) 
{
	if(head = NULL) 
	{
		
	}	
}
